[package]
name = "async_await"
version = "0.1.0"
edition = "2024"

[dependencies]
error-chain = "0.12.4"
reqwest = { version = "0.12", features = ["json", "blocking"] }
tokio = { version = "1.43.0", features = ["full"] }
